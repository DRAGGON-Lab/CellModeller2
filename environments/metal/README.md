# Metal environment

This environment will pin the Apple build and test toolchain once the first
native Metal vertical slice lands. Metal tests require Apple GPU hardware and
must report the device, OS, Xcode, and metallib compiler versions.
