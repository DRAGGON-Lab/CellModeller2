# CUDA environment

This environment will pin the CUDA build and test toolchain once the first
native CUDA vertical slice lands. CUDA tests require NVIDIA hardware and must
report the device, driver, toolkit, and target compute capability.
